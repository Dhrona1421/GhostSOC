[build-system]
requires = ["setuptools==80.9.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ghostsoc"
version = "0.1.0"
description = "GhostSOC unified SOC and policy-controlled response API"
requires-python = ">=3.11,<3.14"
dependencies = [
  "alembic==1.16.5",
  "argon2-cffi==25.1.0",
  "fastapi==0.116.1",
  "httpx==0.28.1",
  "PyJWT==2.10.1",
  "pydantic-settings==2.10.1",
  "psycopg[binary]==3.2.9",
  "PyYAML==6.0.2",
  "reportlab==5.0.0",
  "SQLAlchemy==2.0.43",
  "uvicorn[standard]==0.35.0",
]

[project.optional-dependencies]
test = [
  "pytest==8.4.1",
  "pytest-cov==6.2.1",
  "ruff==0.12.10",
]

[tool.setuptools.packages.find]
include = ["app*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q --strict-markers"
markers = ["integration: tests spanning multiple application services"]

[tool.ruff]
target-version = "py311"
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "S"]
ignore = ["B008", "S101"]

[tool.coverage.run]
source = ["app"]
omit = ["app/main.py"]
